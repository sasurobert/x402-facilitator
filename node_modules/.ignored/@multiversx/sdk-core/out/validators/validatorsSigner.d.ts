import { ValidatorPublicKey, ValidatorSigner } from "../wallet";
export declare class ValidatorsSigners {
    private signers;
    constructor(validatorSigners: ValidatorSigner[]);
    static newFromPem(filePath: string): Promise<ValidatorsSigners>;
    getNumOfNodes(): number;
    getSigners(): ValidatorSigner[];
    getPublicKeys(): ValidatorPublicKey[];
}
