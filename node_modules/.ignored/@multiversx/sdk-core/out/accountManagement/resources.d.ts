import { Address } from "../core/address";
export declare type SetGuardianInput = {
    guardianAddress: Address;
    serviceID: string;
};
export declare type SaveKeyValueInput = {
    keyValuePairs: Map<Uint8Array, Uint8Array>;
};
