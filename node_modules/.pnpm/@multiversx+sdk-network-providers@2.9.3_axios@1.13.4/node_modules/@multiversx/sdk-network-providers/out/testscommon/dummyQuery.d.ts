import { IAddress, IContractQuery } from "../interface";
export declare class MockQuery implements IContractQuery {
    caller: IAddress;
    address: IAddress;
    func: string;
    args: string[];
    value: string;
    constructor(init?: Partial<MockQuery>);
    getEncodedArguments(): string[];
}
